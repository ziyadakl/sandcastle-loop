node_modules/
dist/
.env
# Long-lived Claude subscription token (from `claude setup-token`) for
# container auth on macOS — see docs/adr/0011. Never commit it.
.sandcastle/.oauth.env
*.log
!tests/fixtures/**/*.log
.tsbuildinfo
prd.json.lock
.pnpm-store/
# macOS Finder metadata — never belongs in the repo. Tracked copies were
# untracked in the same commit that added this rule.
.DS_Store
# Local Claude Code tooling/session dir — machine-local, not project state.
.claude/
